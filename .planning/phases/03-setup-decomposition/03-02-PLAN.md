---
phase: 03-setup-decomposition
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - internal/session/setup.go
  - internal/session/setup_container.go
  - internal/session/setup_mounts.go
  - internal/session/setup_postlaunch.go
  - internal/session/setup_toolconfig.go
autonomous: true
requirements: [REFAC-01, REFAC-02]

must_haves:
  truths:
    - "Setup() orchestrator is under 100 lines and calls 4 step functions"
    - "Four new files exist with unexported step functions that contain the extracted logic"
    - "All characterization tests from Plan 01 pass without modification after extraction"
    - "Callers in internal/cli/shell.go and internal/cli/run.go are unaffected (no API change)"
  artifacts:
    - path: "internal/session/setup.go"
      provides: "Thin orchestrator calling resolveContainer, createAndConfigureContainer, postLaunchSetup, configureToolAccess"
      contains: "func Setup("
      min_lines: 30
    - path: "internal/session/setup_container.go"
      provides: "resolveContainer() and createAndConfigureContainer() -- steps 1-5"
      contains: "func resolveContainer("
    - path: "internal/session/setup_mounts.go"
      provides: "setupMounts() moved here, plus workspace mount helpers"
      contains: "func setupMounts("
    - path: "internal/session/setup_postlaunch.go"
      provides: "postLaunchSetup() -- waitForReady, metadata, history, timeout monitor"
      contains: "func postLaunchSetup("
    - path: "internal/session/setup_toolconfig.go"
      provides: "configureToolAccess() plus restoreSessionData, injectCredentials, setupCLIConfig, setupHomeConfigFile moved here"
      contains: "func configureToolAccess("
  key_links:
    - from: "internal/session/setup.go"
      to: "internal/session/setup_container.go"
      via: "resolveContainer(&opts, result)"
      pattern: "resolveContainer\\("
    - from: "internal/session/setup.go"
      to: "internal/session/setup_container.go"
      via: "createAndConfigureContainer(&opts, result)"
      pattern: "createAndConfigureContainer\\("
    - from: "internal/session/setup.go"
      to: "internal/session/setup_postlaunch.go"
      via: "postLaunchSetup(&opts, result)"
      pattern: "postLaunchSetup\\("
    - from: "internal/session/setup.go"
      to: "internal/session/setup_toolconfig.go"
      via: "configureToolAccess(&opts, result, skipLaunch)"
      pattern: "configureToolAccess\\("
---

<objective>
Extract the 4 code regions from Setup() into step functions across 4 new files, then reduce Setup() to a thin orchestrator under 100 lines. All characterization tests from Plan 01 must pass without modification after extraction.

Purpose: REFAC-01 requires the decomposition into composable functions. REFAC-02 requires the orchestrator under ~80 lines. This is a purely structural change -- no behavior changes.

Output: 4 new Go source files in internal/session/, modified setup.go as thin orchestrator.
</objective>

<execution_context>
@/workspace/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-setup-decomposition/03-CONTEXT.md
@.planning/phases/03-setup-decomposition/03-RESEARCH.md
@.planning/phases/03-setup-decomposition/03-01-SUMMARY.md

@internal/session/setup.go
@internal/session/types.go
@internal/session/security.go

<interfaces>
<!-- Current Setup() structure to decompose. Line numbers reference setup.go as of Plan 01 completion (unmodified). -->

Lines 115-124: Logger default (stays in Setup orchestrator)
Lines 126-164: Steps 1-3 -> resolveContainer(&opts, result) error
  - Container naming (126-137)
  - Image determination (139-153)
  - Execution context: RunAsRoot, HomeDir (155-164)

Lines 166-356: Steps 4-5 -> createAndConfigureContainer(&opts, result) (skipLaunch bool, err error)
  - Container existence check (166-218)
  - Container creation: init, UID mapping, workspace mount, tmpfs, mounts, security mounts, limits, start (220-356)
  - Returns skipLaunch to control downstream behavior

Lines 358-402: Steps 6-7 -> postLaunchSetup(&opts, result) error
  - waitForReady (358-362)
  - Metadata labels (364-372)
  - History recording (374-383)
  - Timeout monitor (385-402)

Lines 404-476: Steps 9-11 -> configureToolAccess(&opts, result, skipLaunch)
  - Resume/restore session data (404-432)
  - Skip launch log (434-437)
  - CLI tool config setup (439-472)
  - No error return -- all errors are logged as warnings

Helper functions to MOVE (not rewrite):
  - setupMounts() (58-78) -> setup_mounts.go
  - waitForReady() (478-501) -> setup_postlaunch.go
  - restoreSessionData() (503-534) -> setup_toolconfig.go
  - injectCredentials() (536-604) -> setup_toolconfig.go
  - setupCLIConfig() (606-808) -> setup_toolconfig.go
  - setupHomeConfigFile() (810-866) -> setup_toolconfig.go
  - hasLimits() (868-886) -> setup_container.go (only used in createAndConfigureContainer)

Helper functions to KEEP in setup.go:
  - isColimaOrLimaEnvironment() (22-45) -- used across steps, general utility
  - buildJSONFromSettings() (47-55) -- used by multiple tool config helpers

Types to KEEP in setup.go:
  - SetupOptions (80-98)
  - SetupResult (100-109)
  - DefaultImage, ClincusImage constants (17-20)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract step functions into 4 new files</name>
  <files>internal/session/setup_container.go, internal/session/setup_mounts.go, internal/session/setup_postlaunch.go, internal/session/setup_toolconfig.go</files>
  <read_first>
    - internal/session/setup.go (the full file -- every line matters for correct extraction)
    - internal/session/types.go (MountConfig, MountEntry types)
    - internal/session/security.go (SetupSecurityMounts, GetProtectedPathsForLogging called from setup_container.go)
    - internal/container/commands.go (IncusExec function signature)
    - internal/container/manager.go (Manager methods used in setup)
    - internal/limits/limits.go (ApplyResourceLimits, ApplyOptions, ParseDuration, NewTimeoutMonitor signatures)
    - internal/tool/tool.go (Tool interface, ToolWithHomeConfigFile, ToolWithEssentialFiles interfaces)
  </read_first>
  <action>
Create 4 new files. Each file is `package session` and contains the extracted functions with their required imports. NO behavior changes -- copy code verbatim, only changing variable references to use the function parameters.

**File 1: internal/session/setup_container.go**

```go
package session

import (... // only imports needed by these functions)
```

Contains:
1. `func resolveContainer(opts *SetupOptions, result *SetupResult) error` -- Extract lines 126-164 of Setup(). The function:
   - Sets result.ContainerName from opts.ContainerName or ContainerName(opts.WorkspacePath, opts.Slot)
   - Creates result.Manager = container.NewManager(containerName)
   - Determines image, checks ImageExists, sets result.Image
   - Sets result.RunAsRoot and result.HomeDir based on image type
   - Returns errors for image check failure and image not found

2. `func createAndConfigureContainer(opts *SetupOptions, result *SetupResult) (bool, error)` -- Extract lines 166-356 of Setup(). Returns `(skipLaunch bool, err error)`. The function:
   - Checks existing container (exists/running logic)
   - Creates new container if !skipLaunch: init, UID mapping, workspace mount, tmpfs, setupMounts, security mounts, resource limits, start
   - Sets result.ContainerWorkspacePath inside the !skipLaunch block
   - Returns skipLaunch and any error

3. `func hasLimits(cfg *config.LimitsConfig) bool` -- Move verbatim from setup.go lines 868-886.

**File 2: internal/session/setup_mounts.go**

Contains:
1. `func setupMounts(mgr *container.Manager, mountConfig *MountConfig, useShift bool, logger func(string)) error` -- Move verbatim from setup.go lines 58-78. No signature change.

**File 3: internal/session/setup_postlaunch.go**

Contains:
1. `func postLaunchSetup(opts *SetupOptions, result *SetupResult) error` -- Extract lines 358-402 of Setup(). The function:
   - Calls waitForReady(result.Manager, 30, opts.Logger)
   - Sets metadata labels if opts.Tool != nil
   - Records history
   - Sets up timeout monitor if configured, assigns to result.TimeoutMonitor
   - Returns errors from waitForReady and invalid max_duration

2. `func waitForReady(mgr *container.Manager, maxRetries int, logger func(string)) error` -- Move verbatim from setup.go lines 478-501. No signature change.

**File 4: internal/session/setup_toolconfig.go**

Contains:
1. `func configureToolAccess(opts *SetupOptions, result *SetupResult, skipLaunch bool)` -- Extract lines 404-472 of Setup(). Note: this function has NO error return. All errors in this section are logged as warnings, not propagated. The function:
   - Handles resume/restore (lines 404-432)
   - Logs skip-launch message (lines 434-437)
   - Handles CLI tool config setup with all the branching logic (lines 439-472)

2. `func restoreSessionData(...)` -- Move verbatim from setup.go lines 503-534. Same signature.
3. `func injectCredentials(...)` -- Move verbatim from setup.go lines 536-604. Same signature.
4. `func setupCLIConfig(...)` -- Move verbatim from setup.go lines 606-808. Same signature.
5. `func setupHomeConfigFile(...)` -- Move verbatim from setup.go lines 810-866. Same signature.

**Critical extraction rules:**
- Do NOT change any error messages (characterization tests pin them)
- Do NOT change any log messages
- Do NOT change function signatures for moved helper functions
- Do NOT rename the isFileBased closure in configureToolAccess -- keep it as a local closure
- The `skipLaunch` variable is computed inside createAndConfigureContainer and passed explicitly to configureToolAccess as a parameter
- `result.ContainerWorkspacePath` is only set inside the `!skipLaunch` block in createAndConfigureContainer -- preserve this behavior exactly (see Research pitfall 5 about ContainerWorkspacePath)
- Each file must have correct imports. Use `goimports` or ensure imports match the functions' dependencies.
  </action>
  <verify>
    <automated>cd /workspace && go build ./internal/session/ 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - internal/session/setup_container.go contains `func resolveContainer(opts *SetupOptions, result *SetupResult) error`
    - internal/session/setup_container.go contains `func createAndConfigureContainer(opts *SetupOptions, result *SetupResult) (bool, error)`
    - internal/session/setup_container.go contains `func hasLimits(`
    - internal/session/setup_mounts.go contains `func setupMounts(`
    - internal/session/setup_postlaunch.go contains `func postLaunchSetup(opts *SetupOptions, result *SetupResult) error`
    - internal/session/setup_postlaunch.go contains `func waitForReady(`
    - internal/session/setup_toolconfig.go contains `func configureToolAccess(opts *SetupOptions, result *SetupResult, skipLaunch bool)`
    - internal/session/setup_toolconfig.go contains `func restoreSessionData(`
    - internal/session/setup_toolconfig.go contains `func injectCredentials(`
    - internal/session/setup_toolconfig.go contains `func setupCLIConfig(`
    - internal/session/setup_toolconfig.go contains `func setupHomeConfigFile(`
    - `go build ./internal/session/` exits 0 (compiles without errors)
  </acceptance_criteria>
  <done>4 new files created with all extracted step functions and moved helpers. Package compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Setup() as thin orchestrator and verify all tests pass</name>
  <files>internal/session/setup.go</files>
  <read_first>
    - internal/session/setup.go (current state after Task 1 extraction -- verify what remains)
    - internal/session/setup_container.go (resolveContainer, createAndConfigureContainer signatures)
    - internal/session/setup_postlaunch.go (postLaunchSetup signature)
    - internal/session/setup_toolconfig.go (configureToolAccess signature)
    - internal/session/setup_container_test.go (characterization tests that must still pass)
    - internal/session/setup_mounts_test.go (characterization tests that must still pass)
    - internal/session/setup_postlaunch_test.go (characterization tests that must still pass)
    - internal/session/setup_toolconfig_test.go (characterization tests that must still pass)
  </read_first>
  <action>
Rewrite `setup.go` to contain ONLY:

1. Package declaration and imports
2. Constants: `DefaultImage`, `ClincusImage`
3. Utility functions that stay: `isColimaOrLimaEnvironment()` (lines 22-45), `buildJSONFromSettings()` (lines 47-55)
4. Type definitions: `SetupOptions`, `SetupResult` (keep exactly as-is)
5. Thin `Setup()` orchestrator:

```go
// Setup initializes a container for a session.
// This configures the container with workspace mounting and user setup.
func Setup(opts SetupOptions) (*SetupResult, error) {
    result := &SetupResult{}

    // Default logger
    if opts.Logger == nil {
        opts.Logger = func(msg string) {
            fmt.Fprintf(os.Stderr, "[setup] %s\n", msg)
        }
    }

    // Steps 1-3: Resolve container name, image, execution context
    if err := resolveContainer(&opts, result); err != nil {
        return nil, err
    }

    // Steps 4-5: Check existing container, create/configure/start if needed
    skipLaunch, err := createAndConfigureContainer(&opts, result)
    if err != nil {
        return nil, err
    }

    // Steps 6-7: Wait for ready, set metadata, record history, start timeout
    if err := postLaunchSetup(&opts, result); err != nil {
        return nil, err
    }

    // Steps 9-11: Restore session, inject credentials, setup tool config
    configureToolAccess(&opts, result, skipLaunch)

    opts.Logger("Container setup complete!")
    return result, nil
}
```

6. Remove the `//nolint:gocyclo` directive from Setup() -- no longer needed since the function is simple.

**Remove from setup.go** (all moved to new files in Task 1):
- `setupMounts()` function (moved to setup_mounts.go)
- `waitForReady()` function (moved to setup_postlaunch.go)
- `restoreSessionData()` function (moved to setup_toolconfig.go)
- `injectCredentials()` function (moved to setup_toolconfig.go)
- `setupCLIConfig()` function (moved to setup_toolconfig.go)
- `setupHomeConfigFile()` function (moved to setup_toolconfig.go)
- `hasLimits()` function (moved to setup_container.go)
- All the inline code from Setup() lines 126-472 (now in step functions)

**Remove unused imports** from setup.go. The thin orchestrator only needs: `fmt`, `os`. The `encoding/json`, `path/filepath`, `strings`, `time`, and internal package imports move to the new files where they're used.

**Verification sequence (run in order):**
1. `go build ./internal/session/` -- must compile
2. `go test ./internal/session/ -count=1 -v` -- ALL tests must pass (characterization + existing)
3. `go vet ./internal/session/` -- no issues
4. `sed -n '/^func Setup/,/^}/p' internal/session/setup.go | wc -l` -- must be under 100 lines
5. `go test ./... -count=1` -- full project test suite passes

**If any characterization test fails:** The extraction has a bug. Do NOT modify the test. Fix the extracted function to match the original behavior exactly.
  </action>
  <verify>
    <automated>cd /workspace && go build ./internal/session/ && go test ./internal/session/ -count=1 -v 2>&1 | tail -40 && echo "---" && sed -n '/^func Setup/,/^}/p' internal/session/setup.go | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./internal/session/` exits 0
    - `go test ./internal/session/ -count=1` exits 0 (all characterization tests pass)
    - `go vet ./internal/session/` exits 0
    - `sed -n '/^func Setup/,/^}/p' internal/session/setup.go | wc -l` outputs a number less than 100
    - setup.go does NOT contain `func setupMounts(`
    - setup.go does NOT contain `func waitForReady(`
    - setup.go does NOT contain `func restoreSessionData(`
    - setup.go does NOT contain `func hasLimits(`
    - setup.go contains `resolveContainer(&opts, result)`
    - setup.go contains `createAndConfigureContainer(&opts, result)`
    - setup.go contains `postLaunchSetup(&opts, result)`
    - setup.go contains `configureToolAccess(&opts, result, skipLaunch)`
    - setup.go does NOT contain `nolint:gocyclo`
    - `go test ./... -count=1` exits 0 (full project tests pass)
  </acceptance_criteria>
  <done>Setup() is a thin orchestrator under 100 lines. All 4 step functions in separate files. All characterization tests pass unchanged. Full project test suite green. No API changes to SetupOptions or SetupResult.</done>
</task>

</tasks>

<verification>
Complete phase verification:

```bash
# All session tests pass
go test ./internal/session/ -count=1 -v

# Setup() orchestrator is under 100 lines
sed -n '/^func Setup/,/^}/p' internal/session/setup.go | wc -l

# 4 new source files exist
ls internal/session/setup_container.go internal/session/setup_mounts.go internal/session/setup_postlaunch.go internal/session/setup_toolconfig.go

# Full project tests pass
go test ./... -count=1

# No race conditions
go test ./internal/session/ -count=1 -race

# Lint passes
make lint
```
</verification>

<success_criteria>
- Setup() orchestrator under 100 lines (REFAC-02)
- 4 new files with step functions: setup_container.go, setup_mounts.go, setup_postlaunch.go, setup_toolconfig.go (REFAC-01)
- All characterization tests pass unchanged after extraction (REFAC-03 validated)
- SetupOptions and SetupResult unchanged (no API breakage)
- Full project test suite green
- go vet and go build clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-setup-decomposition/03-02-SUMMARY.md`
</output>
